generator client {
  provider = "prisma-client"
  output   = "../lib/prisma"
}

datasource db {
  provider = "postgresql"
}

model Repository {
  id               String @id
  owner            String
  name             String
  issueCount       Int
  pullRequestCount Int

  @@unique(name: "github_ref", [owner, name])
}
