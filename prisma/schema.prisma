generator client {
  provider = "prisma-client"
  output   = "../lib/prisma"
}

datasource db {
  provider = "postgresql"
}

model Repository {
  owner            String
  name             String
  issueCount       Int    @default(0)
  pullRequestCount Int    @default(0)

  @@id([owner, name])
}
